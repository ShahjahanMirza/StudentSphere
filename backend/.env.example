# MongoDB Atlas Connection (Cloud Database) - RECOMMENDED
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/student_records?retryWrites=true&w=majority

# For local MongoDB (alternative)
# MONGODB_URI=mongodb://localhost:27017/student_records

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (for future authentication features)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# CORS Origin (Frontend URL)
CORS_ORIGIN=http://localhost:3001
