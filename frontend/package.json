{
  "name": "student-record-frontend",
  "version": "1.0.0",
  "description": "Frontend for Student Record Management System",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3001 --host=localhost --no-browser",
    "serve": "live-server --port=3001",
    "build": "echo \"Static HTML/CSS/JS - no build needed\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["student", "record", "management", "frontend", "html", "css", "javascript"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
